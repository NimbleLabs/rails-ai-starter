Your <%= @app_name %> admin account is ready

You're an admin of <%= @app_name %>. Set a password to sign in with <%= @user.email %>:
<%= @set_password_url %>

Then open the admin any time at <%= @admin_url %>

The link works for <%= @hours %> hours. After that, use "Forgot your password?" on the sign-in page.
